# NixOS system configuration placeholder for proxima
#
# This file is NOT used in standalone mode.
# proxima uses home-manager only; system config is managed separately
# via /etc/nixos/configuration.nix
#
# If you want to manage NixOS config via this flake in the future:
# 1. Change mode = "standalone" to mode = "nixos" in flake/home-manager.nix
# 2. Move/merge your /etc/nixos config here
# 3. Import hardware-configuration.nix

{ ... }:
{
  # Placeholder - not used in standalone mode
}
